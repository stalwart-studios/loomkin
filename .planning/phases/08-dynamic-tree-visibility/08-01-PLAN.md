---
phase: 08-dynamic-tree-visibility
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - test/loomkin/tools/team_spawn_test.exs
  - test/loomkin/teams/agent_child_teams_test.exs
  - test/loomkin_web/live/workspace_live_tree_test.exs
  - test/loomkin_web/live/team_tree_component_test.exs
autonomous: true
requirements:
  - TREE-01
  - TREE-02
must_haves:
  truths:
    - "Four new test files exist with @moduletag :skip and @tag :skip stubs"
    - "Each stub file has failing test placeholders for the behaviors its plan implements"
    - "mix test runs without compile errors on all new files"
  artifacts:
    - path: "test/loomkin/tools/team_spawn_test.exs"
      provides: "Stubs for TREE-02: signal NOT published from tool after migration"
    - path: "test/loomkin/teams/agent_child_teams_test.exs"
      provides: "Stubs for TREE-02: terminate/2 calls Manager.dissolve_team for each child"
    - path: "test/loomkin_web/live/workspace_live_tree_test.exs"
      provides: "Stubs for TREE-01: team_tree assign updated on signal, dissolution walk"
    - path: "test/loomkin_web/live/team_tree_component_test.exs"
      provides: "Stubs for TREE-01: component renders, open/close, node selection"
  key_links:
    - from: "test stub files"
      to: "Plans 08-02 through 08-05"
      via: "remove @moduletag :skip and implement tests in later plans"
      pattern: "@moduletag :skip"
---

<objective>
Create Wave 0 failing test stub files for all Phase 8 behaviors.

Purpose: Nyquist compliance — every behavior has a test file before implementation starts. Later plans fill in the implementations and remove the :skip tags.
Output: 4 test files with @moduletag :skip stubs covering TREE-01 and TREE-02.
</objective>

<execution_context>
@/Users/vinnymac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vinnymac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-dynamic-tree-visibility/08-CONTEXT.md
@.planning/phases/08-dynamic-tree-visibility/08-RESEARCH.md

<interfaces>
<!-- Wave 0 stub pattern established in Phase 5 and Phase 7 -->
<!-- @moduletag :skip at module level skips all tests in the file -->
<!-- @tag :skip on each individual test for per-test readability -->
<!-- assert false placeholder marks tests as intentionally failing if unskipped -->

Existing test file pattern (from nested_teams_test.exs, team_broadcaster_test.exs):
```elixir
defmodule Loomkin.Teams.NestedTeamsTest do
  use ExUnit.Case, async: false
  # ...setup...
  test "..." do
    # ...
  end
end
```

Wave 0 stub pattern (from Phase 5, Phase 7):
```elixir
defmodule Loomkin.Teams.AgentChildTeamsTest do
  use ExUnit.Case, async: false
  @moduletag :skip

  describe "terminate/2 child team dissolution" do
    @tag :skip
    test "dissolves all spawned child teams on terminate" do
      assert false
    end
  end
end
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create team_spawn_test.exs and agent_child_teams_test.exs stubs</name>
  <files>test/loomkin/tools/team_spawn_test.exs, test/loomkin/teams/agent_child_teams_test.exs</files>
  <action>
Create two stub files using the Wave 0 pattern: `@moduletag :skip` at module level, `@tag :skip` on each individual test, `assert false` placeholder.

**test/loomkin/tools/team_spawn_test.exs** — covers TREE-02:
- Module: `Loomkin.Tools.TeamSpawnTest`
- `use ExUnit.Case, async: false`
- describe "ChildTeamCreated signal not published from tool" with:
  - `@tag :skip; test "TeamSpawn.run/2 does not publish ChildTeamCreated after migration" do; assert false; end`
  - `@tag :skip; test "ChildTeamCreated is published exactly once by Manager.create_sub_team/3" do; assert false; end`

**test/loomkin/teams/agent_child_teams_test.exs** — covers TREE-02:
- Module: `Loomkin.Teams.AgentChildTeamsTest`
- `use ExUnit.Case, async: false`
- describe "spawned_child_teams field" with:
  - `@tag :skip; test "spawned_child_teams defaults to empty list" do; assert false; end`
  - `@tag :skip; test "receiving :child_team_spawned message adds team_id to spawned_child_teams" do; assert false; end`
- describe "terminate/2 child team dissolution" with:
  - `@tag :skip; test "terminate/2 calls Manager.dissolve_team for each spawned child team" do; assert false; end`
  - `@tag :skip; test "terminate/2 with no child teams completes without error" do; assert false; end`
  </action>
  <verify>
    <automated>mix test test/loomkin/tools/team_spawn_test.exs test/loomkin/teams/agent_child_teams_test.exs --include skip 2>&1 | grep -E "skipped|compiled|error" | head -10</automated>
  </verify>
  <done>Both files compile without errors; mix test reports tests as skipped (not errors)</done>
</task>

<task type="auto">
  <name>Task 2: Create workspace_live_tree_test.exs and team_tree_component_test.exs stubs</name>
  <files>test/loomkin_web/live/workspace_live_tree_test.exs, test/loomkin_web/live/team_tree_component_test.exs</files>
  <action>
Create two stub files using the same Wave 0 pattern.

**test/loomkin_web/live/workspace_live_tree_test.exs** — covers TREE-01:
- Module: `LoomkinWeb.WorkspaceLiveTreeTest`
- `use LoomkinWeb.ConnCase` (matches existing workspace_live test pattern)
- describe "team_tree assign" with:
  - `@tag :skip; test "team_tree is empty map on mount with no sub-teams" do; assert false; end`
  - `@tag :skip; test "team_tree updated on :child_team_created signal" do; assert false; end`
  - `@tag :skip; test "team_tree removes dissolved team and its descendants" do; assert false; end`
  - `@tag :skip; test "workspace_live subscribes to child team on ChildTeamCreated signal" do; assert false; end`

**test/loomkin_web/live/team_tree_component_test.exs** — covers TREE-01:
- Module: `LoomkinWeb.TeamTreeComponentTest`
- `use LoomkinWeb.ConnCase`
- describe "TeamTreeComponent" with:
  - `@tag :skip; test "component is hidden when team_tree is empty" do; assert false; end`
  - `@tag :skip; test "component renders trigger button when sub-teams exist" do; assert false; end`
  - `@tag :skip; test "toggle_tree opens and closes the dropdown" do; assert false; end`
  - `@tag :skip; test "selecting a tree node sends switch_team to parent" do; assert false; end`
  </action>
  <verify>
    <automated>mix test test/loomkin_web/live/workspace_live_tree_test.exs test/loomkin_web/live/team_tree_component_test.exs --include skip 2>&1 | grep -E "skipped|compiled|error" | head -10</automated>
  </verify>
  <done>Both files compile without errors; mix test reports tests as skipped</done>
</task>

</tasks>

<verification>
`mix test` (full suite) reports no new compile errors or unexpected failures. All 4 new files present in the file tree.
</verification>

<success_criteria>
4 test stub files exist, compile cleanly, and are individually runnable. No production code changed.
</success_criteria>

<output>
After completion, create `.planning/phases/08-dynamic-tree-visibility/08-01-SUMMARY.md`
</output>
